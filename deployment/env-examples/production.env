# Production Environment Example
# Copy to root as .env.production for production deployment

COMPOSE_PROFILES=production
ENVIRONMENT=production
NODE_ENV=production
FRONTEND_DOCKERFILE=Dockerfile

# ============================================
# REPLACE WITH YOUR DOMAIN AND EMAIL:
# ============================================
DOMAIN=yourdomain.com
ACME_EMAIL=your-email@yourdomain.com

# Traefik Configuration
TRAEFIK_ENABLE=true

# Backend URL for Frontend (uses your domain)
NEXT_PUBLIC_BACKEND_URL=https://api.${DOMAIN}

# Resource Limits (production)
REDIS_MEMORY=512mb

# Port Configuration (internal only with Traefik)
BACKEND_PORT=8000
FRONTEND_PORT=3000
REDIS_PORT=6379

# ============================================
# ADD YOUR PRODUCTION ENVIRONMENT VARIABLES:
# ============================================

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-production-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-production-supabase-service-role-key
POSTGRES_URL_NON_POOLING=your-production-postgres-connection

# AI Configuration
GEMINI_API_KEY=your-production-gemini-api-key
